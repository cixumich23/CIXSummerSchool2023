def assign(gui_elements, current_task, max_capacity, num_slots):
    gui_elements_to_display = []

    for i in range (0, num_slots):
        gui_elements_to_display.append(gui_elements[i])

    return gui_elements_to_display
